// writing an code to print hello world
#include<stdio.h>
int main()
{
    printf("Hello World\n");
    // int x = (7>6 ? 1:0);
    // printf("%d",x);
    int x=1,y=0;
    ( x>=y && ! x && x* y < 0 && y==0)
    ? printf("yes") : printf("no");
    return 0;
}